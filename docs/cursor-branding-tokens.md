# Branding tokens (pointer)

Canonical brand reference: [brand.md](./brand.md).

This file exists so craft-pass prompts can link a stable path. Colour primitives, contrast remediation, type scale, and shape tokens are defined in `src/app/globals.css` and documented in `docs/brand.md`.
