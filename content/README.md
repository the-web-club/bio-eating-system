# Content catalogue (Phase D)

Dietitian-authored content only. The portal renders keys from `content/en.json` and subfolders when authored.

| Folder | Purpose |
|--------|---------|
| `meals/` | Meal templates replacing heuristic assembly |
| `replacements/` | Ranked replacement copy per reason |
| `adaptation/` | Life-happened response copy |
| `lessons/` | Learn tab bodies |
| `pricing/` | Weekly cost tables |

Do not generate nutrition prose in code. Missing keys show empty states.
